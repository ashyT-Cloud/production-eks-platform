apiVersion: v1
kind: Service

metadata:
  name: {{ include "fittrack.fullname" . }}-frontend
  labels:
    {{- include "fittrack.labels" . | nindent 4 }}

spec:
  type: {{ .Values.frontend.service.type }}

  selector:
    app: frontend

  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.service.port }}
      protocol: TCP
